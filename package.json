{
  "name": "antd-ui-modifier",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "agent": "node --experimental-strip-types src/agent/server.ts",
    "build:overlay": "esbuild src/overlay/index.ts --bundle --format=iife --outfile=dist/overlay.js"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "ts-morph": "^23.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
